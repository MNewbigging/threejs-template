export enum SceneName {
  BASIC_TRANSFORMS = 'Basic transforms',
  BASIC_ORBIT_CAM = 'Basic orbit camera',
  STANDARD_ORBIT_CAM = 'Standard orbit camera',
  BUFFER_GEOMETRY = 'Buffer geometry',
  BASIC_TEXTURE = 'Basic texture',
  MATERIALS = 'Materials',
  TEXT = 'Text',
  CANNON_PHYSICS = 'CannonJs Physics',
}

export const sceneNames = Object.values(SceneName);
